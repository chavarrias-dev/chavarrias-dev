/**
 * Default Claude model for PDF field extraction (facturas, constancias, etc.).
 * Override with env `ANTHROPIC_MODEL` where implemented.
 */
export const ANTHROPIC_PDF_MODEL = "claude-haiku-4-5";
